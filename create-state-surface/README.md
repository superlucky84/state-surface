# create-state-surface

Project scaffolding CLI for StateSurface.

## Usage

```bash
npx create-state-surface my-app
cd my-app
pnpm install
pnpm dev
```

## Important

- Use this CLI only for **new project creation**.
- To update an existing project, run:

```bash
pnpm up state-surface
```
